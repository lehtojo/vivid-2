{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "by-gdb",
			"request": "launch",
			"name": "Vivid Debug",
			"program": "${workspaceRoot}/build/vc.exe",
			"cwd": "${workspaceRoot}",
			//"programArgs": "${workspaceFolder}/box/main.v minimum.math.obj minimum.memory.obj minimum.tests.obj -a -l kernel32.dll -o box -verbose"

			// Build the standard library (Windows)
			//"programArgs": "${workspaceFolder}/libv/ ${workspaceFolder}/libv/windows-x64 minimum.math.obj minimum.memory.obj minimum.tests.obj -static -o v"

			// Build the compiler itself
			//"programArgs": "${workspaceFolder} ${workspaceFolder}/entries/vivid.v -a -o vc -l kernel32 -l v"
			"programArgs": "${workspaceFolder} ${workspaceFolder}/entries/vivid.v ${workspaceFolder}/optimizer/ ${workspaceFolder}/tests/assert.v ${workspaceFolder}/libv/ ${workspaceFolder}/libv/windows-x64/ ${workspaceFolder}/libv/allocator/allocator.v ${workspaceFolder}/minimum.math.obj ${workspaceFolder}/minimum.memory.obj ${workspaceFolder}/minimum.tests.obj -a -o v1 -l kernel32.dll -verbose -O2"
		},
	]
}